# TODO

## セキュリティ・依存関係

- [ ] 公開・デプロイ前に `npm audit` を再実行する。
  - 2026-05-16 時点では `postcss` の moderate 警告が `next` 経由で出ている。
  - 内容は PostCSS の CSS stringify 出力に関する XSS 警告。
  - 現在の試作・ローカル開発では開発を止めるほどではない。
  - `npm audit fix --force` は使わない。audit の提案が `next@9.3.3` への大きな巻き戻しになっており、現行の `next@15.5.18` から戻すのは不適切。
  - 対応方針は、公開前に Next.js / PostCSS の修正版へ通常更新できるか確認する。

## 次の開発ステップ

- [ ] スマホ実機またはブラウザのモバイル表示で、地図・サイドパネル・固定タイムラインの操作性を確認する。
- [ ] データ精度を上げる前に [data_accuracy.md](./data_accuracy.md) の未精査項目を確認する。
- [ ] Step 7: 関東 JR 主要路線 v1 の `needs-verification` を路線ごとに精査する。

## MVP 追加要件

- [x] スマホでは地図を画面の 2/3 以上にする。
- [x] スマホでは再生操作を薄い下部バーとして常時表示する。
- [x] スマホでは再生バーの機能を再生/停止と年代スライダーに限定する。
