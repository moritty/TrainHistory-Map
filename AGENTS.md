# TrainHistory-Map Codex 指示

## 使用言語

- 説明、相談、ドキュメント、ユーザー向け回答では日本語を標準の作業言語とします。
- ユーザーが明示的に指定した場合、またはソースコード、コマンド、ログ、外部仕様の都合で必要な場合のみ、他の言語を使用します。

## プロジェクト概要

- プロジェクト名: `TrainHistory-Map`
- 目的: 日本の鉄道路線が計画・建設・開業・廃止されていく過程を、地図とタイムラインで時系列に可視化する Web アプリを作る。
- 作成背景: 単なる現在の鉄道路線図ではなく、都市と交通インフラが時間をかけてどう形成されたかを見られる歴史・地理・都市開発ビジュアライザーを目指す。
- 想定ユーザー: 鉄道・地図・都市開発・歴史に興味がある一般ユーザー、教育用途、都市開発・不動産・交通インフラ調査用途。
- MVP 対象: 山手線と東京中心部の主要路線をサンプルに、年代スライダー、状態別路線表示、駅表示、イベント年表、路線詳細表示を実装する。
- MVP テーマ: 山手線を対象にする。
- MVP 起点: 1885 年 3 月 1 日に日本鉄道品川線として開業した品川〜赤羽間を、山手線形成史の最初の表示対象にする。
- 地図表現: 淡色の通常地図を基本にする。
- 時間軸: MVP では 2 年刻みスライダーを基本にする。
- 操作 UI: 年代スライダーと再生操作は常に画面下部に表示する。
- スマホ対応: MVP でもスマホで地図・サイド情報・タイムライン操作が破綻しないレイアウトを用意する。
- 将来像: 新橋で汽車が走り始めた時代から現代まで、日本の鉄道網がどのように伸び、増えていったかを動画のように見られる体験を目指す。
- 技術スタック候補: Next.js、React、TypeScript、Tailwind CSS、Leaflet、MapLibre GL JS、静的 JSON / GeoJSON。
- 地図実装方針: 静的サンプルでは Leaflet と国土地理院の淡色地図タイルを使う。将来の本実装では MapLibre GL JS への移行も検討する。
- 初期データ方針: Next.js アプリでは `public/data` 配下に `railway_segments.json`、`stations.json`、`events.json`、`year_summaries.json` を置く。DB は後回しにする。
- データ精度方針: 初期 MVP は概念デモとして扱い、現在線形ベースの簡略 GeoJSON と手作業の最小データから始める。

## ルール管理

- このプロジェクト固有のルール、判断、運用方針は、この `AGENTS.md` に追記します。
- 広すぎる前提よりも、小さく具体的なルールを優先します。
- ユーザーが明示的に依頼しない限り、既存ルールを上書きまたは削除しません。

## セキュリティと情報の取り扱い

- シークレット、認証情報、トークン、API キー、秘密鍵、パスワード、Cookie、認証ヘッダー、`.env` の値をチャットに表示しません。
- 機密値に言及する必要がある場合は、`[REDACTED]` としてマスクします。
- シークレット、ローカル認証情報、秘密鍵、`.env` ファイルを Git に追加しません。
- シークレットを含む可能性があるファイルを扱う場合は、`.gitignore` を確認します。
- ログは機密情報を含む可能性があるものとして扱います。
- ユーザーが目的と範囲を明示的に承認しない限り、プロジェクトファイル、ログ、認証情報、設定値を外部サービスに送信しません。
- ユーザーの明示的な承認なしに、本番環境の操作、デプロイ、破壊的なデータベース変更、課金に関わる変更、大量データ更新を行いません。
- 明示的に依頼されない限り、`rm -rf`、`git reset --hard`、`git clean`、データベース削除、広範囲の上書き操作などの破壊的コマンドを実行しません。

## 編集方針

- 広範囲の変更を行う前に、既存構成を確認します。
- 編集範囲はユーザーの依頼に沿って限定します。
- ユーザーが明示的に依頼しない限り、ユーザーが作成したファイルや変更を削除または巻き戻しません。
- コードや設定を変更した場合は、利用可能な範囲で妥当な確認コマンドを実行します。
